export class Test {
    private variableOne: string;

    public variabletwo: number;

    constructor() {}

    public onMethodOne(numericValue: number): string {
        return numericValue + "";
    }
}